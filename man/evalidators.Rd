% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-eval.R
\name{evalidators}
\alias{evalidators}
\title{Combine validator outputs}
\usage{
evalidators(L)
}
\arguments{
\item{L}{A list of validator outputs. Each element should contain at least
\code{rowid}, \code{variable}, and \code{reason}.}
}
\value{
A \code{data.table} with columns \code{variable}, \code{reason}, and \code{rowid} when
validation succeeds. If validation fails, a \code{data.frame} with \code{rowid},
\code{variable}, and \code{reason}.
}
\description{
\code{evalidators()} combines the output of several validators into the format
used by DataEntry inspectors. Rows with the same \code{variable} and \code{reason} are
grouped together, and their \code{rowid} values are collapsed into a comma-separated
string.
}
\details{
If the validator outputs cannot be combined, \code{evalidators()} returns a
validator-shaped error explaining that the inspector failed.
}
\examples{
L = list(
  data.table::data.table(rowid = c(1, 2), variable = "a", reason = "missing"),
  data.table::data.table(rowid = 3, variable = "a", reason = "missing"),
  data.table::data.table(rowid = 2, variable = "b", reason = "invalid")
)

evalidators(L)

}
